<PhoenixKitWeb.Components.LayoutWrapper.app_layout
  socket={@socket}
  flash={@flash}
  phoenix_kit_current_scope={assigns[:phoenix_kit_current_scope]}
  page_title={gettext("Modules")}
  page_subtitle={gettext("Manage modules")}
  current_path={@url_path}
  project_title={@project_title}
  current_locale={@current_locale}
>
  <div class="flex flex-col px-4 py-6">
    <%!-- Dependency warnings --%>
    <div :if={@dep_warnings != []} class="mb-4 space-y-2">
      <div
        :for={w <- @dep_warnings}
        class="alert alert-warning flex items-center gap-2 py-2 text-sm"
      >
        <.icon name="hero-exclamation-triangle" class="w-4 h-4 shrink-0" />
        <span>
          <strong>{w.module_name}</strong>
          {gettext("requires the")} <strong>{w.requires_key}</strong>
          {gettext("module to be enabled. Some features may not work correctly.")}
        </span>
      </div>
    </div>

    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      <%!-- Storage Module --%>
      <%= if "storage" in @accessible_modules do %>
        <% cfg = @module_configs["storage"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Storage")}
          description={
            gettext(
              "Distributed file storage system with multi-location redundancy and automatic failover"
            )
          }
          icon="💾"
          enabled={cfg[:module_enabled]}
          toggle_event="toggle_module"
          toggle_key="storage"
          show_toggle={false}
        >
          <:status_badges>
            <span class="badge badge-success z-0">{gettext("Always Enabled")}</span>
            <span class="badge badge-info z-0">{gettext("Core System")}</span>
          </:status_badges>

          <:action_buttons>
            <.link
              navigate={PhoenixKit.Utils.Routes.path("/admin/settings/media")}
              class="btn btn-primary btn-sm"
            >
              <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
            </.link>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-2 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Total Buckets")}:</span>
                <span class="font-medium">{cfg[:buckets_count]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Active")}:</span>
                <span class="font-medium">{cfg[:active_buckets_count]}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Referral Codes Module --%>
      <%= if "referrals" in @accessible_modules do %>
        <% cfg = @module_configs["referrals"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Referral Codes")}
          description={gettext("Manage referral codes for user registration and monitoring")}
          icon="🎫"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="referrals"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:enabled] do %>
              <span class="badge badge-outline ml-2 whitespace-nowrap">
                {if cfg[:required], do: gettext("Required"), else: gettext("Optional")}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <%= if cfg[:enabled] do %>
              <.link
                navigate={PhoenixKit.Utils.Routes.path("/admin/settings/referral-codes")}
                class="btn btn-primary btn-sm"
              >
                <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
              </.link>
              <.link
                navigate={PhoenixKit.Utils.Routes.path("/admin/users/referral-codes")}
                class="btn btn-outline btn-sm"
              >
                <.icon name="hero-users" class="w-4 h-4 mr-1" /> {gettext("Manage Codes")}
              </.link>
            <% else %>
              <.link
                navigate={PhoenixKit.Utils.Routes.path("/admin/settings/referral-codes")}
                class="btn btn-outline btn-sm"
              >
                <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
              </.link>
            <% end %>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-2 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Max per user")}:</span>
                <span class="font-medium">{cfg[:max_codes_per_user]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Max uses")}:</span>
                <span class="font-medium">{cfg[:max_uses_per_code]}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Languages Module --%>
      <%= if "languages" in @accessible_modules do %>
        <% cfg = @module_configs["languages"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Languages")}
          description={gettext("Manage language module and translations")}
          icon="🌐"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="languages"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:enabled] do %>
              <span class="badge badge-outline ml-2 whitespace-nowrap">
                {if cfg[:default_language],
                  do: cfg[:default_language].name,
                  else: gettext("No Default")}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <%= if cfg[:enabled] do %>
              <.link
                navigate={PhoenixKit.Utils.Routes.path("/admin/settings/languages")}
                class="btn btn-primary btn-sm"
              >
                <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
              </.link>
            <% else %>
              <.link
                navigate={PhoenixKit.Utils.Routes.path("/admin/settings/languages")}
                class="btn btn-outline btn-sm"
              >
                <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
              </.link>
            <% end %>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-2 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Total languages")}:</span>
                <span class="font-medium">{cfg[:language_count]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Enabled")}:</span>
                <span class="font-medium">{cfg[:enabled_count]}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Comments Module --%>
      <%= if "comments" in @accessible_modules do %>
        <% cfg = @module_configs["comments"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Comments")}
          description={
            gettext(
              "Threaded comments with likes, moderation, and reactions across all content types"
            )
          }
          icon="💬"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="comments"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:enabled] && cfg[:pending_comments] > 0 do %>
              <span class="badge badge-warning ml-2 whitespace-nowrap">
                {gettext("%{count} pending", count: cfg[:pending_comments])}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:enabled] do %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/comments")}
                  class="btn btn-primary btn-sm"
                >
                  <.icon name="hero-chat-bubble-left-right" class="w-4 h-4 mr-1" />
                  {gettext("Manage Comments")}
                </.link>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/settings/comments")}
                  class="btn btn-outline btn-sm"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Settings")}
                </.link>
              <% else %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/settings/comments")}
                  class="btn btn-outline btn-sm"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                </.link>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-3 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Total")}:</span>
                <span class="font-medium">{cfg[:total_comments]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Published")}:</span>
                <span class="font-medium">{cfg[:published_comments]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Pending")}:</span>
                <span class="font-medium">{cfg[:pending_comments]}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Customer Support Module --%>
      <%= if "customer_support" in @accessible_modules do %>
        <% cfg = @module_configs["customer_support"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Customer Support")}
          description={
            gettext("Customer support ticketing system with internal notes and status tracking")
          }
          icon="🎫"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="customer_support"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:enabled] && cfg[:open_tickets] > 0 do %>
              <span class="badge badge-warning ml-2 whitespace-nowrap">
                {gettext("%{count} open", count: cfg[:open_tickets])}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:enabled] do %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/customer-support/tickets")}
                  class="btn btn-primary btn-sm"
                >
                  <.icon name="hero-ticket" class="w-4 h-4 mr-1" /> {gettext("Manage Tickets")}
                </.link>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/settings/customer-support")}
                  class="btn btn-outline btn-sm"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Settings")}
                </.link>
              <% else %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/settings/customer-support")}
                  class="btn btn-outline btn-sm"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                </.link>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-3 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Total")}:</span>
                <span class="font-medium">{cfg[:total_tickets]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Open")}:</span>
                <span class="font-medium">{cfg[:open_tickets]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("In Progress")}:</span>
                <span class="font-medium">{cfg[:in_progress_tickets]}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Connections Module --%>
      <%= if "connections" in @accessible_modules do %>
        <% cfg = @module_configs["connections"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Connections")}
          description={
            gettext("Social relationships with one-way follows and mutual connections")
          }
          icon="🤝"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="connections"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:enabled] do %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/connections")}
                  class="btn btn-primary btn-sm"
                >
                  <.icon name="hero-users" class="w-4 h-4 mr-1" /> {gettext("Overview")}
                </.link>
              <% else %>
                <span class="text-xs text-base-content/50 text-center">
                  {gettext("Enable module to manage connections")}
                </span>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-3 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Follows")}:</span>
                <span class="font-medium">{cfg[:follows_count]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Connections")}:</span>
                <span class="font-medium">{cfg[:connections_count]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Blocks")}:</span>
                <span class="font-medium">{cfg[:blocks_count]}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Crawlers Module --%>
      <%= if "crawlers" in @accessible_modules do %>
        <% cfg = @module_configs["crawlers"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Crawlers")}
          description={
            gettext("Control how search engines and AI bots crawl and index this site")
          }
          icon="🕷️"
          enabled={cfg[:module_enabled]}
          toggle_event="toggle_module"
          toggle_key="crawlers"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:module_enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:module_enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
          </:status_badges>

          <:action_buttons>
            <%= if cfg[:module_enabled] do %>
              <.link
                navigate={
                  PhoenixKit.Utils.Routes.path("/admin/settings/crawlers",
                    locale: @current_locale || "en"
                  )
                }
                class="btn btn-sm w-full btn-primary"
              >
                <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
              </.link>
            <% else %>
              <span class="text-xs text-base-content/50 text-center">
                {gettext("Enable module to access settings")}
              </span>
            <% end %>
          </:action_buttons>

          <:stats>
            <div class="text-sm">
              <span class="text-base-content/70">{gettext("Noindex")}:</span>
              <span class="ml-2 font-medium">
                {if cfg[:no_index_enabled], do: gettext("On"), else: gettext("Off")}
              </span>
            </div>
            <div class="text-sm">
              <span class="text-base-content/70">{gettext("Blocked groups")}:</span>
              <span class="ml-2 font-medium">
                {length(cfg[:blocked_groups] || [])}
              </span>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Sitemap Module --%>
      <%= if "sitemap" in @accessible_modules do %>
        <% cfg = @module_configs["sitemap"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Sitemap")}
          description={
            gettext("Generate XML sitemaps for search engines with styled browser display")
          }
          icon="🗺️"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="sitemap"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:enabled] && cfg[:schedule_enabled] do %>
              <span class="badge badge-outline ml-2 whitespace-nowrap">
                {gettext("Auto-regenerate")}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:enabled] do %>
                <.link
                  navigate={
                    PhoenixKit.Utils.Routes.path("/admin/settings/sitemap",
                      locale: :none
                    )
                  }
                  class="btn btn-sm btn-primary"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                </.link>
                <.link
                  href={PhoenixKit.Utils.Routes.path("/sitemap.xml", locale: :none)}
                  target="_blank"
                  class="btn btn-outline btn-sm"
                >
                  <.icon name="hero-arrow-top-right-on-square" class="w-4 h-4 mr-1" /> {gettext(
                    "Sitemap"
                  )}
                </.link>
              <% else %>
                <span class="text-xs text-base-content/50 text-center">
                  {gettext("Enable module to access settings")}
                </span>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-2 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("URLs")}:</span>
                <span class="font-medium">{cfg[:url_count]}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Last gen")}:</span>
                <span class="font-medium">
                  {format_timestamp(cfg[:last_generated])}
                </span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Pages module remains disabled but code retained for future use --%>

      <%!-- Under Construction (Maintenance Mode) Module --%>
      <%= if "maintenance" in @accessible_modules do %>
        <% cfg = @module_configs["maintenance"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Maintenance Mode")}
          description={
            gettext("Show maintenance page to non-admin users while you work on the site")
          }
          icon="🚧"
          enabled={cfg[:module_enabled]}
          toggle_event="toggle_module"
          toggle_key="maintenance"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:module_enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:module_enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:module_enabled] do %>
              <span class={[
                "badge badge-outline ml-2 whitespace-nowrap",
                if(cfg[:enabled], do: "badge-warning", else: "badge-info")
              ]}>
                {if cfg[:enabled],
                  do: gettext("Maintenance Active"),
                  else: gettext("Maintenance Off")}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:module_enabled] do %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/settings/maintenance")}
                  class="btn btn-sm btn-primary"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                </.link>
                <div class="text-xs text-base-content/70 mt-2">
                  <strong>{gettext("Header")}:</strong>
                  {cfg[:header]}
                </div>
                <div class="text-xs text-base-content/70">
                  <strong>{gettext("Message")}:</strong>
                  {cfg[:subtext]}
                </div>
              <% else %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/settings/maintenance")}
                  class="btn btn-sm btn-outline"
                >
                  <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                </.link>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="text-xs text-base-content/70">
              {if cfg[:module_enabled],
                do: gettext("Activate maintenance mode in settings to block non-admin users"),
                else: gettext("Enable module to access maintenance mode settings")}
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Jobs Module --%>
      <%= if "jobs" in @accessible_modules do %>
        <% cfg = @module_configs["jobs"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Jobs")}
          description={gettext("View-only dashboard for background job status and history")}
          icon="⚙️"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="jobs"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <%= if cfg[:enabled] && cfg[:stats].executing > 0 do %>
              <span class="badge badge-outline badge-warning ml-2 whitespace-nowrap">
                {gettext("%{count} running", count: cfg[:stats].executing)}
              </span>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:enabled] do %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/jobs")}
                  class="btn btn-primary btn-sm"
                >
                  <.icon name="hero-queue-list" class="w-4 h-4 mr-1" /> {gettext("View Jobs")}
                </.link>
              <% else %>
                <span class="text-xs text-base-content/50 text-center">
                  {gettext("Enable module to view background jobs")}
                </span>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-4 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Available")}:</span>
                <span class="font-medium">{cfg[:stats].available}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Completed")}:</span>
                <span class="font-medium">{cfg[:stats].completed}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Retryable")}:</span>
                <span class="font-medium">{cfg[:stats].retryable}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Discarded")}:</span>
                <span class="font-medium">{cfg[:stats].discarded}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Notifications Module --%>
      <%= if "notifications" in @accessible_modules do %>
        <% cfg = @module_configs["notifications"] || %{} %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={gettext("Notifications")}
          description={gettext("Per-user in-app notifications driven by the activity log")}
          icon="🔔"
          enabled={cfg[:enabled] || false}
          toggle_event="toggle_module"
          toggle_key="notifications"
        >
          <:status_badges>
            <span class={[
              "badge",
              if(cfg[:enabled], do: "badge-success", else: "badge-neutral")
            ]}>
              {if cfg[:enabled], do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col gap-2 w-full">
              <%= if cfg[:enabled] do %>
                <.link
                  navigate={PhoenixKit.Utils.Routes.path("/admin/notifications")}
                  class="btn btn-primary btn-sm"
                >
                  <.icon name="hero-bell" class="w-4 h-4 mr-1" /> {gettext("View Notifications")}
                </.link>
              <% else %>
                <span class="text-xs text-base-content/50 text-center">
                  {gettext("Enable module to view notifications")}
                </span>
              <% end %>
            </div>
          </:action_buttons>

          <:stats>
            <div class="grid grid-cols-3 gap-2 text-xs">
              <div>
                <span class="text-base-content/70">{gettext("Total")}:</span>
                <span class="font-medium">{cfg[:total] || 0}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Unread")}:</span>
                <span class="font-medium">{cfg[:unread] || 0}</span>
              </div>
              <div>
                <span class="text-base-content/70">{gettext("Dismissed")}:</span>
                <span class="font-medium">{cfg[:dismissed] || 0}</span>
              </div>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>

      <%!-- Legal Module --%>
      <%!-- External Modules (auto-discovered) --%>
      <%= for ext <- @external_modules, ext.key in @accessible_modules do %>
        <PhoenixKitWeb.Components.Core.ModuleCard.module_card
          title={Gettext.gettext(PhoenixKitWeb.Gettext, ext.name)}
          description={Gettext.gettext(PhoenixKitWeb.Gettext, ext.description)}
          icon={ext.icon}
          enabled={ext.enabled}
          toggle_event="toggle_module"
          toggle_key={ext.key}
        >
          <:status_badges>
            <span class={[
              "badge",
              if(ext.enabled, do: "badge-success", else: "badge-neutral")
            ]}>
              {if ext.enabled, do: gettext("Enabled"), else: gettext("Disabled")}
            </span>
            <span class="badge badge-outline">v{ext.version}</span>
            <span class="badge badge-info badge-outline">{gettext("External")}</span>
            <%= for req_mod <- ext.required_modules do %>
              <%= unless mcfg(@module_configs, req_mod, :enabled, false) do %>
                <span class="badge badge-warning">
                  {gettext("Requires %{module}", module: String.capitalize(req_mod))}
                </span>
              <% end %>
            <% end %>
            <%!-- Module-specific badges from config --%>
            <%= if ext.enabled do %>
              <%= if Map.has_key?(ext.config, :save_body) do %>
                <span class="badge badge-outline whitespace-nowrap">
                  {if ext.config[:save_body],
                    do: gettext("Body Saved"),
                    else: gettext("Headers Only")}
                </span>
              <% end %>
            <% end %>
          </:status_badges>

          <:action_buttons>
            <div class="flex flex-col items-stretch space-y-2 w-full">
              <%= if ext.enabled do %>
                <%= if ext.settings_path do %>
                  <.link
                    navigate={PhoenixKit.Utils.Routes.path(ext.settings_path)}
                    class="btn btn-primary btn-sm"
                  >
                    <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                  </.link>
                <% end %>
                <%= if ext.admin_links != [] do %>
                  <div class="flex flex-wrap gap-2">
                    <%= for link <- ext.admin_links do %>
                      <.link
                        navigate={PhoenixKit.Utils.Routes.path(link.path)}
                        class="btn btn-outline btn-sm flex-1"
                      >
                        <%= if link.icon do %>
                          <.icon name={link.icon} class="w-4 h-4 mr-1" />
                        <% end %>
                        {link.label}
                      </.link>
                    <% end %>
                  </div>
                <% end %>
              <% else %>
                <%= if ext.settings_path do %>
                  <.link
                    navigate={PhoenixKit.Utils.Routes.path(ext.settings_path)}
                    class="btn btn-outline btn-sm"
                  >
                    <.icon name="hero-cog-6-tooth" class="w-4 h-4 mr-1" /> {gettext("Configure")}
                  </.link>
                <% end %>
              <% end %>
            </div>
          </:action_buttons>

          <:stats :if={function_exported?(ext.module, :module_stats, 0)}>
            <div class="grid grid-cols-2 gap-2 text-xs">
              <%= for stat <- ext.module.module_stats() do %>
                <div>
                  <span class="text-base-content/70">
                    {if is_binary(stat.label),
                      do: Gettext.gettext(PhoenixKitWeb.Gettext, stat.label),
                      else: to_string(stat.label)}:
                  </span>
                  <span class="font-medium">{stat.value}</span>
                </div>
              <% end %>
            </div>
          </:stats>
        </PhoenixKitWeb.Components.Core.ModuleCard.module_card>
      <% end %>
    </div>

    <%!-- Hex was unreachable. Rendered separately from the list below, because an
         empty list and "we could not ask" look identical to an operator and
         only one of them means there is nothing to install. --%>
    <div :if={@catalog_status == :unavailable} class="mt-8">
      <h2 class="text-lg font-semibold mb-3 text-base-content/70">
        {gettext("Available Packages")}
      </h2>
      <div class="alert alert-warning">
        <.icon name="hero-exclamation-triangle" class="w-5 h-5" />
        <span>
          {gettext("Couldn't reach hex.pm, so the package list is unavailable right now.")}
          <a href={@hex_browse_url} target="_blank" rel="noopener" class="link">
            {gettext("Browse PhoenixKit packages on hex.pm")} →
          </a>
        </span>
      </div>
    </div>

    <%!-- Available Packages (known but not installed) --%>
    <%= if @not_installed_packages != [] do %>
      <div class="mt-8">
        <h2 class="text-lg font-semibold mb-3 text-base-content/70">
          {gettext("Available Packages")}
        </h2>
        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          <%= for pkg <- @not_installed_packages do %>
            <div class="card bg-base-100 shadow border-2 border-dashed border-base-300">
              <div class="card-body">
                <div class="flex items-center gap-3">
                  <.icon name={pkg.icon} class="w-8 h-8 opacity-60" />
                  <div class="flex-1">
                    <h3 class="card-title text-base-content/70">
                      {Gettext.gettext(PhoenixKitWeb.Gettext, pkg.name)}
                    </h3>
                    <p class="text-sm text-base-content/50">
                      {Gettext.gettext(PhoenixKitWeb.Gettext, pkg.description)}
                    </p>
                  </div>
                </div>

                <div class="divider my-2"></div>

                <div class="flex items-center justify-between gap-2 flex-wrap">
                  <span class="badge badge-ghost badge-outline">{gettext("Not installed")}</span>
                  <a
                    :if={pkg.hex_url}
                    href={pkg.hex_url}
                    target="_blank"
                    class="btn btn-xs btn-outline"
                  >
                    <.icon name="hero-arrow-top-right-on-square" class="w-3 h-3" /> Hex.pm
                  </a>
                </div>

                <div class="mt-2">
                  <p class="text-xs text-base-content/40 mb-1">{gettext("Add to mix.exs")}:</p>
                  <code class="block text-xs bg-base-200 rounded p-2 font-mono">
                    {~s|{:#{pkg.package}, "~> 0.1"}|}
                  </code>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <%!-- Module Management Help --%>
    <div class="alert alert-info mt-6">
      <.icon name="hero-information-circle" class="w-5 h-5" />
      <div>
        <h3 class="font-bold">{gettext("Module Management")}</h3>
        <p class="text-sm">
          {gettext(
            "Each module can be enabled or disabled using the toggle switches above. Click \"Configure\" to access detailed settings for each module on dedicated pages."
          )}
        </p>
      </div>
    </div>
  </div>
</PhoenixKitWeb.Components.LayoutWrapper.app_layout>
